<%= semantic_form_for [@theater, @space] do |f| %>
  <%= f.inputs :name, :address, :seating_capacity, :calendar %>


  <%= f.actions do %>
    <%= f.action :submit, :as => :input %>
  <% end %>
<% end %>
