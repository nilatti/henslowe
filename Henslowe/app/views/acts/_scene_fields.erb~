<p class="fields">
<%= f.input :scene_number  %>
<%= f.input :scene_summary, :input_html => { :rows => 2 } %>
<%= f.input :play_id, :as => :hidden, :value => @act.play_id %>
</p>
  <%= f.fields_for :french_scenes %>
  <%= f.link_to_add "Add a french scene", :french_scenes %>
<%= f.link_to_remove "Remove Scene" %>

