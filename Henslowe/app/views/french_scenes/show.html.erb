<% title "French Scene" %>

<p>
  <strong>Number:</strong>
  <%= @french_scene.act.act_number %>.<%= @french_scene.scene.scene_number %>.<%= @french_scene.fs_number %><br />
</p>
  <strong>Characters:</strong>
  <ul>
  <% @french_scene.characters.each do |c| %>
    <li><%= c.name %></li>
  <% end %>
  </ul>
<p>
  <%= link_to "Edit", edit_french_scene_path(@french_scene) %> |
  <%= link_to "Destroy", @french_scene, :confirm => 'Are you sure?', :method => :delete %> |
  <%= link_to "View All", french_scenes_path %>
</p>
