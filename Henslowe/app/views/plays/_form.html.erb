<%= semantic_nested_form_for @play do |f| %>
  <%#= f.input :author_id, :as => :select, :collection => Author.all %><br />
  <%= f.input :title %>
  <%= f.input :publication_date %>
  <%= f.input :genres, :as => :check_boxes, :collection => Genre.all %>
  <%= f.input :summary %>
  <%= f.fields_for :acts %>
  <%= f.link_to_add "Add an act", :acts %>
<%= f.buttons %>
<% end %>
