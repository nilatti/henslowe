<p class="fields">
<%= f.input :fs_number %><%= f.link_to_remove "Remove French Scene" %>
<%= f.input :characters, :as => :check_boxes, :collection => Character.find(:all, :conditions => { :play_id => @play.id }) %>
<%= f.input :play_id, :as => :hidden, :value => @play.id %>
<%= f.input :act_id, :as => :hidden, :value => @a %>
</div>
</p>
